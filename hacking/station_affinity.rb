require 'date'

trips = '201402_trip_data.csv'
stations = '201402_station_data.csv'

File.open(stations).each_with_index { |line, index|
	unless index == 0
	end
}

File.open(trips).each_with_index { |line, index|
	unless index == 0
	end
}
